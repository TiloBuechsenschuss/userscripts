# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

See [AGENTS.md](./AGENTS.md) for the project's architecture, conventions, and the
userscript distribution rules (notably: bump `@version` on every user-facing change, and
keep each file's path in sync with its `@downloadURL`).
